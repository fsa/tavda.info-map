---
import Layout from "../layouts/Layout.astro";
import Sidebar from "../components/Sidebar";
import "../styles/sidebar.css";
import "../styles/map.css";
---

<Layout title="Карта города — Тавда">
  <div class="wrapper">
    <Sidebar client:load />
    <div class="main-container">
      <div id="map"></div>
    </div>
  </div>

  <script>
    // Записываем состояние маркера из localStorage до гидратации React,
    // чтобы серверный HTML (showMarker=false) был заменён на клиентский.
    (window as any).__INITIAL_SHOW_MARKER__ =
      localStorage.getItem("tavda:showUserMarker") === "true";

    import { initMap } from "../lib/map";
    const instance = initMap("map");
    (window as any).__map = instance;
    window.dispatchEvent(new CustomEvent("map:ready", { detail: instance }));
  </script>
</Layout>
